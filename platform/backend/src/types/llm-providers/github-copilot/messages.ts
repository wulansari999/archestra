/**
 * GitHub Copilot message schemas - OpenAI-compatible
 *
 * GitHub Copilot uses an OpenAI-compatible API, so we re-export OpenAI schemas.
 */
export { MessageParamSchema, ToolCallSchema } from "../openai/messages";
